{
    "name": "@cheqd/creds-auth",
    "private": true,
    "scripts": {
        "package:sync": "node templates/sync-to-current.js",
        "package:update": "node templates/sync-from-current.js",
        "pnpm:devPreinstall": "pnpm package:sync",
        "preinstall": "npx only-allow pnpm",
        "ci": "pnpm i --frozen-lockfile --fix-lockfile",
        "build": "pnpm -r build",
        "lint": "pnpm -r lint",
        "test": "pnpm -r test"
    },
    "devDependencies": {
        "@types/node": "^18.14.6",
        "husky": "^8.0.3",
        "ts-node": "^10.9.1",
        "typescript": "^4.9.5"
    },
    "workspaces": {
        "packages": [
            "packages/*"
        ]
    }
}
